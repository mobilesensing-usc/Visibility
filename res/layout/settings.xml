<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
  xmlns:android="http://schemas.android.com/apk/res/android"
  android:layout_width="wrap_content"
  android:layout_height="wrap_content"
  android:id="@+id/settingsLayout"   >
  <TextView  
 	android:id="@+id/privacyS1"
 	android:layout_above="@+id/privacyText"
    android:layout_width="wrap_content" 
    android:layout_height="wrap_content"
    android:layout_alignLeft="@+id/privacyText"
    android:textStyle="bold" 
    android:text="Privacy Filter"
    android:textSize="17.0px"    
    android:paddingRight="20dip"
    
    />
    
<ToggleButton
	android:id="@+id/privacyFilter"
	android:layout_above="@+id/privacyText"
	android:layout_toRightOf="@+id/privacyS"
 	android:layout_width="100dip"
    android:layout_height="wrap_content"
    
    
	/>
<!--  <TextView
	android:id="@+id/privacyText"
	android:layout_centerInParent="true"
	android:layout_width="300px"
	android:paddingTop="10dip"
	android:layout_height="wrap_content"
	android:text="If the filter  is on, GPS Co-ordinates will not be shared."
	android:textSize="20dip"/>-->
</LinearLayout>
